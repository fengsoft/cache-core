# Changesets

Use Changesets to describe public package changes before release.

## Workflow

1. make the code change
2. run `bunx changeset`
3. choose the affected package
4. describe the impact
5. commit the generated markdown file

